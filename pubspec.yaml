name: crux_ui
description: >-
  A playful Flutter UI kit. Ships design tokens (colors, spacing,
  typography) and a theme layer today; widgets are still to come.
version: 0.1.0

topics:
  - ui
  - widgets
  - design-system

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
