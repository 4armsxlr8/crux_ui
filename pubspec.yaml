name: crux_ui
description: >-
  A playful Flutter UI kit. Ships design tokens (colors, spacing,
  typography), a theme layer, and a CruxButton atom.
version: 0.2.0

topics:
  - ui
  - widgets
  - design-system

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  motor: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
