/// Crux UI — a playful Flutter UI kit.
///
/// This package is under development and does not export any widgets yet.
/// This file is the single public entry point of the package: every component
/// that Crux UI ships in the future will be exported from here, so consumers
/// only ever need one import.
///
/// ```dart
/// import 'package:crux_ui/crux_ui.dart';
/// ```
library;
